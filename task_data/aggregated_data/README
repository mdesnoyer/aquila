########################################################################
# 			TASK DATA												   #
########################################################################

There are two files with the primary task data:

idx_2_id:
	A CSV-file of the form:
		<index, id>
	That permits the indices in win_data to be mapped to actual images.

win_data.npy
	A numpy-formatted data file consisting of a matrix where each row
	has the form:
		idx_a, idx_b, wins_ab, wins_ba, prior_ab, prior_ba

	Where:
		idx_a: The index of image A
		idx_b: The index of image B
		wins_ab: The number of wins, A over B
		wins_ba: The number of wins, B over A
		prior_ab: The target prior, A over B
		prior_ba: The target prior, B over A